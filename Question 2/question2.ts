let person : string = "Arhama" // this saves the name
console.log(person.toLowerCase()) ; // shows the name in all small letters
console.log(person.toUpperCase()) ; // shows the name in all capital letters
console.log(person.charAt(0).toUpperCase() + person.slice(1).toLowerCase()) ; // showsthe first letter big
