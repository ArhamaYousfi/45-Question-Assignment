let usernames: string[] = [];

if (usernames.length === 0 ) {
    console.log("We need to find some users!");
} else {
    // greet users
}
// removing all usernames ensures the message "We need to find some users!" is printes